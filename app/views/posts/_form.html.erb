
	<%= simple_form_for @post, url: posts_path do |f| %>
     <%= f.input :title, as: :string %>
		<%= f.input :sub_title, as: :pagedown, :rows => 5, :cols => 18  %>
   	<%= f.input :body, as: :pagedown, :rows => 16, :cols => 16 %>
		<%= f.submit :submit, class: "btn btn-default" %>

	 <% end %>
